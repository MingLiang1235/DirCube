todo 3.1.6.3:
1、添加一个刷新按钮，该按钮不改变任何值，就是利用devide重新计算一下各个矩形的值，然后重绘一下，以解决窗口的矩形绘制出来有时不是正好填满
	工作区的问题。
2、考虑既然递归获取了各级数值，那么向下钻取的时候就不应该再次扫描硬盘，（目前c盘子目录二次扫描速度就很慢）
而应在第一次递归中生成一个树形结构，保存各级目录的内容，钻取时使用
已建立的结构，除非明确点击了扫描按钮（用于硬盘内容发生了变动）
	这就需要一个很好的数据结构，既能记录各级目录本级递归和的结果，还有一颗树记录各级目录间的关系。在重新扫描硬盘的时候，还能更新各级目录及更新树的目录结构关系。
     或许会很麻烦，因为树的结构也会更新（删除了某目录，或者拷贝/移动某目录至某分支等等）
3、可以加入显示不显示空闲空间的二态钮。
另外磁盘用量，此目录用量，空闲空间用量都应可在console显示：
这些需求都不重要，做不做都可，都可以不做。
todo 3.1.6.1:
1、方格避免过小过细
2、利用用户查询自动生成地图，并且标注时间戳。如果再次查询覆盖了已有查询，直接从数据库中提取已有查询。双击是直接钻取（不读取硬盘），点击scan才强制读取硬盘。
3、图标使用96x96试一下
4、解决闪烁的最好办法可能是自己组织一个方框类，然后定义前端后端色彩，然后再绑定单击双击等事件。
5、说实话，我还是认为使用webservice更好，第一是标准，任何一个人都可以替换其客户端和服务端，只需要一个简单的配置就匹配上了。二是就目前来说，还看不到使用了ws之后，多占用了多少系统资源。有可能占用了一定的资源，但对目前的
环境来说，还不是太大的问题。
todo 3.1.6:
1、consoles 该写成只读的。
2、可改变consoles大小。（slid）
3、关闭的时候进行当前状态序列化，打开的时候提取文件进行反序列化设置当前状态。ok
todo 3.1.5:
1、显示几个方块变成可配置的。从12～无限制。  ok
2、尽量优化程序，优化屏显，使小的bug无从出现（比如反复钻取progress不停出现的问题）。优化源代码，将没有必要的源代码删除。 partial ok
3、优化输出，将不必要的输出抹去。 ok  
